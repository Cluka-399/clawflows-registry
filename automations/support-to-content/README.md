# Support to Content

Turns recurring support pain into a ranked content brief list instead of letting the same questions repeat forever.

## Requirements

| Capability | Example Skills |
|------------|----------------|
| `database` | runtime/local workflow state |
| `llm` | summarization/reasoning model |
