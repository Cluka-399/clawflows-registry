# Dependency Updater

Runs a dependency inspection command and turns the output into a prioritized upgrade brief.

## Requirements

| Capability | Example Skills |
|------------|----------------|
| `exec` | runtime shell |
| `llm` | summarization/reasoning model |
