# API Health Monitor

Checks a health endpoint on a schedule and turns the result into a fast human-readable operator summary.

## Requirements

| Capability | Example Skills |
|------------|----------------|
| `web-fetch` | runtime fetch |
| `llm` | summarization/reasoning model |

## Schedule

Default: every 15 minutes.
